[project]
name = "syap"
version = "0.1.0"
description = "?"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=2.3.2",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
    "uv>=0.8.22",
]
